import { DeviceRepository} from "./deviceRepository";

export interface DeviceService {
}

export const deviceServiceFactory = (deviceRepository: DeviceRepository): DeviceService => ({
  
});
